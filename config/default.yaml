app:
  title: My demo API server

server:
  host: "0.0.0.0"
  port: 3000
  logger:
    level: trace
  body_limit: 50mb
  https:
    key: "server.key"
    cert: "server.pem"

temmplating:
  engine: handlebars